@model IEnumerable<AdminMensajeria.Models.GEN_UNIDAD>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DescripcionUnidad)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Codigo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Simbolo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.EstatusUnidad)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DescripcionUnidad)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Codigo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Simbolo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.EstatusUnidad)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdUnidad }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdUnidad }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdUnidad })
        </td>
    </tr>
}

</table>
